import React from "react";
import { Box } from '@mui/material';

const Separator = (props) => (
  <Box sx={{ width: 80, height: 10, backgroundColor: 'primary.main' }} {...props} />
);

export default Separator;
